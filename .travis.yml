sudo: required
services:
  - docker

before_install:
  - docker build -t moatef/react-test -f ./client/Dockerfile.dev ./client

scripts:
  - docker run moatef/react-test npm run test -- --coverage

after_seccess:
  - docker build -t moatef/multi-client ./client 
  - docker biuld -t moatef/multi-nginx ./nginx
  - docker build -t moatef/multi-server ./server
  - docker build -t moatef/multi-worker ./worker

  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  - docker push moatef/multi-client
  - docker push moatef/multi-nginx
  - docker push moatef/multi-server
  - docker push moatef/multi-worker


  - 